language: cpp
os:
- linux

compiler:
- g++
- clang++

addons:
  apt:
    packages:
    - clang

install:
    - sudo apt-get -qq update;
    - sudo apt-get -qq install libboost1.48-all-dev;

script:
    - cmake .; make; make test;
